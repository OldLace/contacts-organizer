

  <h5><%= @contact.first_name %>
    <%= @contact.last_name %></h5>
  <p><%= @contact.phone %></p>
  <p><%= @contact.email %></p>
  <p><%= @contact.address %></p>

  <h6><%= link_to "Edit", edit_contact_path(@contact) %></h6>
  <h6><%= link_to "Delete", contact_path(@contact), data: {confirm: "Are you sure you wish to delete: #{@contact.first_name}?",}, method: :delete %></h6>

 <h6><%= link_to "All Contacts", contacts_path %></h6>


